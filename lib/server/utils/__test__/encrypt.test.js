import { hash, compare } from '../encrypt';

describe('encrypt utils', () => {
  describe('hash', () => {
    it.todo('should create array of ecrypted arguments');

    it.todo('should support string or number data type');

    it.todo('should throw error if data provided is not supported');
  });

  describe('compare', () => {
    it.todo('should create array with every element is true');

    it.todo('should support string or number data type');

    it.todo('should throw error if data provided is not supported');
  });
});
